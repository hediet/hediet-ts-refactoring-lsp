{
	"private": true,
	"workspaces": [
		"language-service-plugin",
		"ts-api-extras",
		"vscode-extension"
	],
	"scripts": {
		"build": "yarn workspace @hediet/ts-api-extras build && yarn workspace @hediet/ts-lsp build && yarn workspace hediet-ts-lsp build"
	}
}
